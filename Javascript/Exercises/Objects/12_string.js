// String Primitive
const message = "This is my first message";

// message.length
// message[0]
// message.includes('my')
// message.startsWith('this)
// message.endWith('')
// message.indexOf('my')
// message.replace('first', 'second')
// message.toUpperCase()
// message.toLowerCase()
// message.trim()
// message.trimLeft()
// message.split(' ')

// String Object
const another = new String("hi");

// Escape Notation
//  (\n, \", \r, etc ) => check the rest on mdn
