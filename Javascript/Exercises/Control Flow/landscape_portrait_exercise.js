// let number = isLandScape(200, 400);
console.log(isLandScape(100, 200));

function isLandScape(width, height) {
  // if (width > height) return true;
  // return false;

  // return (width > height) ? true : false;

  return width > height;
}
