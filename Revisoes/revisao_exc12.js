function gere(min, max) {
  return Math.floor(Math.random() * (max - min) + min); // floor para os num inteiros, o resto serve para ver ointervalo e dar um numero maior ou igual a 0, mas menor q o intervalo
}
