// Num bolo de aniversário existem várias velas.

// Cada elemento do array velas é um booleano que tem o valor  se a vela estiver acesa e  se estiver apagada.

// Implementa a função ordenaVelasApagadasPrimeiro de forma a que o array fique com as velas apagadas no início.


function ordenaVelasApagadasPrimeiro(velas) {
  // TODO
  let result = velas.sort();
  return result;
}